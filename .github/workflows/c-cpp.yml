name: C/C++ CI Build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest,macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: cmake init
      run: cmake .
    - name: make
      run: make
