name: build & test

on:
  push:
    branches: [master]
    paths-ignore:
      - "**/*.md"
      - "LICENSE"
      - "doc/*"
  pull_request:
    branches: [master]
    paths-ignore:
      - "**/*.md"
      - "LICENSE"
      - "doc/*"

jobs:
  build:
    name: CI Build & Test
    runs-on: ${{matrix.os}}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]

    steps:
      - uses: actions/checkout@v2

      - name: 1) Pull submodules
        run: git submodule init && git submodule update

      - name: 2) Build & Test
        run: bash ci/build_test.sh ${{matrix.os}}

      - name: 5) Upload coverage to Codecov (only on Ubuntu)
        if: matrix.os == 'ubuntu-latest'
        run: bash ci/codecov.sh
